export const CONFIG = {
    REFETCH_INTERVAL: 5000,      // ดึงข้อมูลทุก 5 วินาที
    HISTORY_LIMIT: 20,          // เก็บประวัติ Ping ล่าสุด 20 ค่า
    MAX_LOG_ENTRIES: 50,        // เก็บ Log ย้อนหลัง 50 รายการ
    TIMEOUT_THRESHOLD: "TIMEOUT" // สถานะที่ถือว่าล่ม
};